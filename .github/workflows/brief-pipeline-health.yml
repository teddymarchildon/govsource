name: Check brief pipeline health

on:
  schedule:
    - cron: "53 */4 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: govsource-brief-health
  cancel-in-progress: false

jobs:
  run:
    if: ${{ vars.BRIEF_PIPELINE_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: services/data-sync
    env:
      PYTHONUNBUFFERED: '1'
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: services/data-sync/requirements.runtime.txt
      - run: pip install -r requirements.runtime.txt
      - name: Check freshness and persistent failures
        run: python scripts/check_brief_pipeline.py
